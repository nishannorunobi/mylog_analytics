apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki          # fixed uid so provisioned dashboards can bind to it
    access: proxy
    url: http://localhost:3100
    isDefault: true
    jsonData:
      maxLines: 1000
